FINAL HARDENING — UI INPUT + PRECISION + SAFETY
================================================

Read from: airequest/myprompt.txt
Applied 4 remaining gaps from the final master prompt (RULES 4, 8, 9, 11).

---

REMAINING GAP 1: RULE 4 — type="number" allows e/E/+/- (DATA INTEGRITY RISK)
Field: All numeric inputs — test_weight, net_weight, purity, amount
Current: no onKeyDown handler — browser spec allows e, E, +, - in type="number"
Expected: block all invalid characters at keypress level
Fix Applied:
  Added blockInvalidNumericKeys = (e) => { if (['e','E','+','-'].includes(e.key)) e.preventDefault(); }
  Added onKeyDown={blockInvalidNumericKeys} to all 4 numeric inputs

---

REMAINING GAP 2: RULE 9 — clampDecimals preserved garbage on NaN (DATA INTEGRITY RISK)
Current: if (isNaN(n)) return value  →  preserves 'abc', 'e1', etc.
Expected: if (isNaN(n)) return ''    →  clears invalid input, never preserves garbage
Fix Applied:
  if (value === '' || value === null || value === undefined) return '';
  if (isNaN(n)) return '';

---

REMAINING GAP 3: RULE 11 — Backend had no decimal precision enforcement
Fields: purity (max 2dp), test_weight (max 3dp), net_weight (max 3dp)
Current: backend accepted 91.6666 purity, 9.12345678 test_weight — silently truncated by Decimal.js
Expected: reject values exceeding allowed decimal places (same limits as UI)
Fix Applied (testService.js _validateSaveResults):
  Added _assertMaxDecimals(value, maxDp, label) helper
  purity checked: max 2 decimal places
  test_weight checked: max 3 decimal places
  net_weight checked: max 3 decimal places

---

REMAINING GAP 4: RULE 8 — Two remaining '' → 0 coercions
Locations:
  - buildBaseData: total: parseFloat(amount || 0) → amount !== '' ? parseFloat(amount) : 0
  - handleSaveDraft test path: purity: Number(i.purity) || 0 → i.purity !== '' ? Number(i.purity) : 0
  - handleSaveDraft test path: test_weight/net_weight: '' → undefined (not sent) instead of Number('')=0
Fix Applied: All explicit coercions, no silent || 0 patterns remain.

---

FINAL STATUS — ALL 11 RULES

RULE 1: No default values              ✓ (value='' for all numeric fields)
RULE 2: Placeholder is visual only     ✓ (placeholder only, not value)
RULE 3: Input overwrite                ✓ (no prefix bugs)
RULE 4: Invalid character block        ✓ FIXED (onKeyDown blocks e/E/+/-)
RULE 5: Amount precision 2dp           ✓ (onBlur clampDecimals + step="0.01")
RULE 6: Weight precision 3dp           ✓ (onBlur clampDecimals + step="0.001")
RULE 7: Decimal enforcement UI+backend ✓ FIXED (backend _assertMaxDecimals added)
RULE 8: No auto-conversion '' → 0      ✓ FIXED (all coercions explicit)
RULE 9: Safe decimal handling          ✓ FIXED (clampDecimals hardened)
RULE 10: Derived values not auto-fill  ✓ (net_weight value={item.net_weight ?? ''})
RULE 11: Backend parity                ✓ FIXED (_assertMaxDecimals in _validateSaveResults)

Gap tests: 15/15 passing.
