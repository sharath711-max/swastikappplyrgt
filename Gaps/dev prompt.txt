You're right. The current `item:done`/`cert:done` handlers are still unsafe because they don't fetch fresh data. Here is the **exact corrective patch** for `WorkflowBoard.js` – no redesign, just the required change.

---

## 🔴 K2 FINAL CORRECTION – Fetch fresh data on DONE events

### Replace existing `item:done` handler

**BEFORE (remove this):**
```js
socket.on("item:done", ({ id, type }) => {
  moveToDoneColumn(id, type);
});
```

**AFTER (add this):**
```js
socket.on("item:done", async ({ id, type }) => {
  if (pendingIds.current.has(id)) return;

  const fresh = await fetchBoardItem(id, type);

  const current = lastVersions.current[id] || 0;
  if (fresh.version <= current) return;

  lastVersions.current[id] = fresh.version;

  setBoard(prev => replaceAcrossColumns(prev, fresh));
});
```

### Same fix for `cert:done` (if separate)

If you have a `cert:done` event listener, apply identical pattern:

```js
socket.on("cert:done", async ({ id }) => {
  if (pendingIds.current.has(id)) return;

  const fresh = await api.getCertificateById(id);

  const current = lastVersions.current[id] || 0;
  if (fresh.version <= current) return;

  lastVersions.current[id] = fresh.version;

  setBoard(prev => replaceAcrossColumns(prev, fresh));
});
```

### Do NOT change:

- `fetchBoardItem` helper (already exists)
- `replaceAcrossColumns` helper (already exists)
- `pendingIds` / `lastVersions` refs
- Version guard logic structure

### Validation after applying:

- [ ] Click "Finalize" → `item:done` event triggers → board shows latest totals/items (not stale)
- [ ] No duplicate cards
- [ ] Drag & drop still works (no regression)
- [ ] Full board reload does NOT happen

---

**After applying this correction**, reply:

> **K2 fixed**

Then I will provide the final **K3 CertificatePage Save Draft** patch (if not already applied) and the production readiness checklist.