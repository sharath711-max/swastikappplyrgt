## TODO — Migration Gap Tracking

### 🔴 Gap 3 — Idempotency Break (CRITICAL)

**Status:** ☐ Not Verified

**Objective:**
Ensure critical operations are idempotent — repeated execution must not create duplicate effects.

---

**Required Rule:**

```id="a1x3k9"
Same request executed multiple times → same final state
```

---

**Disallowed:**

```id="b7m2q4"
Duplicate ledger entries
Duplicate financial updates
Repeated state transitions
```

---

**Verification Steps:**

Search for protection mechanisms:

```
grep -r "idempotency" backend/
grep -r "request_id" backend/
grep -r "UNIQUE" backend/
```

Check critical flows:

* ☐ certificate finalize
* ☐ saveResults
* ☐ ledger / credit_history inserts
* ☐ payment updates

For each:

* ☐ Is there an idempotency key or request identifier?
* ☐ Is there a UNIQUE constraint preventing duplicates?
* ☐ Is there a logical guard (already processed check)?

---

**Acceptable Protections (at least one must exist):**

* Idempotency key tracking (preferred)
* Database UNIQUE constraints
* Explicit “already processed” guards

---

**Pass Condition:**
Repeated execution does NOT change final state.

---

**Fail Condition:**
Repeated execution creates duplicate records or double financial impact.

---

**Risk if not fixed:**

* Double billing
* Duplicate ledger entries
* Incorrect balances
* Audit inconsistencies
